git init
git add README.md
git commit -m "first commit"
git remote add myorigin https://github.com/revjatto/Movie_Review.git
git push -u morigin master

@movie = Movie.last

@user = User.first
@movie = Movie.find(1)
@movie.user_id = 1
@movie.save
@movie

adding image using paperclip
rails generate paperclip movie image
</p><%= movie.title %></p>
    </p><%= movie.director %></p>
    </p><%= movie.movie_length %></p>
    </p><%= movie.description %></p>
    </p><%= movie.rating %></p>
    <%= link_to 'Show', movie %>
